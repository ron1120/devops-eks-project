apiVersion: apps/v1
kind: Deployment
metadata:
  name: sawe-engine
  labels:
    app.kubernetes.io/name: sawe-engine
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: sawe-engine
  template:
    metadata:
      labels:
        app.kubernetes.io/name: sawe-engine
    spec:
      containers:
        - name: engine
          image: sawe-engine:latest
          ports:
            - containerPort: 8080
          readinessProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 20
