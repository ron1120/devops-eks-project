---
- name: Configure EC2 and deploy Jenkins
  hosts: app
  become: true

  vars:
    docker_image: "jenkins/jenkins:lts"
    container_name: "jenkins"
    jenkins_home: "/var/lib/docker/volumes/jenkins_home/_data"
    jenkins_admin_user: "admin"
    jenkins_admin_password: "admin"

  tasks:
    # ── System Updates ──
    - name: Update and upgrade apt packages
      apt:
        update_cache: true
        upgrade: dist

    # ── Python Installation ──
    - name: Install Python and dependencies
      apt:
        name:
          - python3
          - python3-venv
          - python3-pip
        state: present

    # ── Docker Prerequisites ──
    - name: Install Docker prerequisites
      apt:
        name:
          - apt-transport-https
          - ca-certificates
          - gnupg
          - lsb-release
        state: present

    # ── Docker CE Installation ──
    - name: Download Docker install script
      get_url:
        url: https://get.docker.com
        dest: /tmp/get-docker.sh
        mode: "0755"

    - name: Run Docker install script
      command: sh /tmp/get-docker.sh
      args:
        creates: /usr/bin/docker

    - name: Add ubuntu user to docker group
      user:
        name: ubuntu
        groups: docker
        append: true

    - name: Ensure Docker is started and enabled
      systemd:
        name: docker
        state: started
        enabled: true

    # ── Reset SSH connection to pick up docker group ──
    - name: Reset SSH connection
      meta: reset_connection

    # ── Jenkins Deployment ──
    - name: Pull Jenkins Docker image
      docker_image:
        name: "{{ docker_image }}"
        source: pull

    - name: Run Jenkins container
      docker_container:
        name: "{{ container_name }}"
        image: "{{ docker_image }}"
        state: started
        restart_policy: unless-stopped
        ports:
          - "8080:8080"
          - "50000:50000"
        volumes:
          - jenkins_home:/var/jenkins_home
          - /var/run/docker.sock:/var/run/docker.sock

    # ── Jenkins Container Dependencies ──
    - name: Install Python inside Jenkins container
      command: >
        docker exec -u root {{ container_name }} bash -c
        "apt-get update && apt-get install -y python3 python3-venv python3-pip"
      changed_when: true

    - name: Install Docker prerequisites inside Jenkins container
      command: >
        docker exec -u root {{ container_name }} bash -c
        "apt-get install -y apt-transport-https ca-certificates gnupg lsb-release curl"
      changed_when: true

    - name: Install Docker CE inside Jenkins container
      command: >
        docker exec -u root {{ container_name }} bash -c
        "curl -fsSL https://get.docker.com -o /tmp/get-docker.sh && sh /tmp/get-docker.sh"
      changed_when: true

    - name: Add jenkins user to docker group inside container
      command: >
        docker exec -u root {{ container_name }} bash -c
        "usermod -aG docker jenkins"
      changed_when: true

    - name: Fix Docker socket permissions
      file:
        path: /var/run/docker.sock
        mode: "0666"

    - name: Restart Jenkins container to pick up group changes
      docker_container:
        name: "{{ container_name }}"
        state: started
        restart: true